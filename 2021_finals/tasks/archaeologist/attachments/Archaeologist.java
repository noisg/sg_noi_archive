public class Archaeologist {
    public void archaeologist(int N, int K, int L, int[] map, int lightlevel, int[] paths) {
        // You should fill in this function
    
        // For example, set the light level of the current room
        stub.set_light(0);
        
        // For example, take a corridor to an adjacent room
        stub.RoomPathsPair result = stub.take_path(0);
        int new_room = result.room;
        int[] new_paths = result.paths;
    }
}
